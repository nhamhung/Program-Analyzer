procedure procedure {
  a = (b + c) / 1 * (2 % 3);    // 1
  read d;    // 2
  while ((!((a != b) || (d + 3 >= e))) && (c % 4 / e <= d * 5)) {   // 3
    read d;   // 4
    if (d == d) then {    // 5
      print e;    // 6
      e = e + e * (e - e);    // 7
    } else { 
      print e;    // 8
      while (e != f) {    // 9
        f = e + e * (e - e);    // 10
        if (a + b == f) then {    // 11
          print g;    // 12
        } else {
          print h;    // 13
        }
      }
      print e;    // 14
    }
    read d;   // 15
    d = d;    // 16
  }
  print e;    // 17
  e = d + 3;    // 18
  if (a != c) then {    // 19
    a = (e + d) * c;    // 20
  } else {
    read e;   // 21
  }
}